import React from "react";

function NotFound()
{
    // getData().then(res => console.log(res));
    return (
        <div className="background full-width full-height center-justified 
            center-aligned horizontal largest">
            Not Found
        </div>
    );
}

export default NotFound;